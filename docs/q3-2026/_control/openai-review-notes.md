# OpenAI 采集中审阅记录

本文件是协调任务的直接视觉检查，不代表品牌交付已验收。

## 2026-09-09 第一轮

- 已直接查看 `assets/work-mention/global-plugin-directory.png`：完整插件目录可见，包含应用行与加号；这张图能证明插件目录，不能证明输入 @ 后弹出列表。
- 已直接查看 `_work/work-contact.png`：宣传片包含语音输入、Slack 分析、Sheets 产物、PPT 产物、移动端后续任务。画面之间有明显剪辑，不能当作连续全过程；片中出现 Permissions Ask for approval 文案，仍不能替代真正批准/拒绝门的状态帧。
- `_work/static-manifest.json` 为当前素材提供官方页面与媒体 URL，且将目录图明确标成非 @ 菜单，将 Atlas 2025-10-21 图标为背景。最终仍要确认逐图 source.md、README 与 SOURCES.md 使用一致描述。
- 必查草案标题：任务书 OpenAI 草案含“不切模式”，现有 `desktop-mode-picker.png` 却标为 Work 模式菜单。最终标题必须由实际流程支持，不能照抄草案。应区分进入 Work 需要选模式，与进入后通过 @ 调用外部应用的行为。
- 初始文件检查中缺 source.md 为采集中间态，暂不发送重复催办；最终验收必须补全。文件检查绝不证明素材语义和过程覆盖。

## 2026-09-09 第二轮：Computer Use 画中画

- 本地 `work-pip.info.json` 记录源频道 OpenAI、上传日期 2026-07-10、原视频 56 秒、1920×1080；URL 为 https://www.youtube.com/watch?v=IYYTI73J7Rg 。
- 已用该下载视频生成并直接查看 `_control/openai-pip-review.png`（5 秒采样缩略索引，仅作审阅，不算正式状态帧）。可见 @Computer 请求编辑 Keynote、任务执行中输入区停止控件、桌面应用画中画、用户切到其他网页和表格；宠物图标上有任务数提示。
- 片中切换多个示例，不能合称同一任务连续过程。当前索引未证明用户点击停止/接管、拒绝后的处理，也不能用桌面 Computer Use 画面替代内置浏览器专项 UI。
- 当前材料能支持“实时/画中画过程呈现”，接管和停止操作仍需独立检查。

## 2026-09-09 第三轮：@ 教学帧

- 直接查看 `_work/plugin-67.png`：输入框内 Google Calendar 显示为带图标的蓝色标签，输入区、发送按钮、Plugins 上下文行均可见。这能证明“选中后内联标签”的状态。
- 直接查看 `_work/plugin-73.png`：同一日历任务已列出候选时段，但顶部仍为 Working for 15s、右下仍有停止控件。不能仅凭出现答案就将该帧标成任务完成或退出。
- 上述两帧只显示单个 Google Calendar 引用；任务书还要求“一句话串多个应用怎么显示”，需另有证据或标独立缺口。

## 2026-09-09 文档与文件初审

- 已核对生成的 SOURCES.md 与资产链接：当前 48 个本地素材链接可解析，逐素材 source.md 已覆盖全部正式素材目录。全局参考重复帧已在来源表说明，不计额外流程。
- 当前文件审计为 43 PNG / 5 MP4；合作背景照宽度低于 1280，已要求补官方高分辨率版本或排除正式图片交付，保留原始背景缓存。
- README 对 Work 的模式选择、PPT 修改未完成/已更新、Cloud browser 与 Computer Use、Atlas 背景日期均有边界说明。
- GPT-Live 当前 01–03 来自 startup，04–05 来自 picker，实际为两段演示。正文应明确分组并使用原片时间码，不能让连续编号和 00:00→00:04 的展示暗示一段连续操作。后续补审音视频时一并检查。
- GPT-Live 已下载对话尚未审完、官方 YouTube 网页播放器尚待尝试，均属于可推进工作，不能按公开路径穷尽验收。

## 2026-09-09 补采与媒体验证

- 已将上述两条具体补采路径送达原品牌任务；该任务随后确认官方浏览器演示能在网页播放器以 1080p 全屏播放，正在直接取帧。下载受限不能等同于素材不可取得。
- 当前已生成的 5 个短片均通过完整视频/可用音频流解码，无错误输出；每个文件的 SHA-256 与结果保存在 openai-decode-audit.json。此证据仅证明这些文件可解码，不能证明缺失交互状态已补齐。文件变化后需重新核验相应项。

## 2026-09-09 网页播放器新帧直接审阅

- 直接查看 `_work/browser-15.png`：输入框含 Browser 标签和搜索社区反馈的委托；Approve for me 仍只是模式标签。
- 直接查看 `_work/browser-20.png`：任务侧面的浏览器页已显示社区搜索结果；片中有局部放大，不能将其当作全窗口总览。
- 直接查看 `_work/browser-35.png`：Annotating 状态、标题选区与批注输入框可见，用户请求寻找相似案例。此帧支持“基于页面对象的人工批注”，仍不单独证明用户接管代理鼠标或代理已停止。
- 新帧在 `_work` 仍为候选；正式采纳时必须保留原 YouTube URL、实际播放器时间码与全局截图。与此前桌面 Keynote 画中画分组，不以两段不同表面凑单一连续过程。

## 2026-09-09 第三方审批图直接审阅

- 直接查看 `thirdparty-windows-02-access-approval.png`：ChatGPT Windows 窗口显示承認待ち；Browser 卡明确请求 help.openai.com 访问，包含拒否、一度だけ許可，以及所有网站授权入口。源作者灰色遮挡和红框保留。
- 直接查看 `thirdparty-note-broad-approval-confirm.png`：note.com 访问审批卡上方有扩大到任意网站且无需再次确认的弹窗，包含取消和所有网站允许。它证明扩大权限的二次确认，不能泛称单一动作的 Always allow。
- 两图来自不同作者/案例，不能拼成连续流程；现有同一 Windows 案例仅有运行与待批准两张。拒绝后的实际结果仍无图，PARTIAL 合理，不能标四帧流程完成。
- 来源表分别标注 note.com 第一手上手、2026-08-15 / 2026-08-31、原图 URL 和未本次实测，且注明第二篇图文表述存在差异、以图为准。接受作为第三方视觉补充；最终正文保留官方权限文档依据与第三方标识。

## 最终处置验收

任务已实际完成（cursor153）。读取最终七段README、coverage、SOURCES、QA与EVIDENCE-DISPOSITION。补看图片对话12秒选图实拍和Atlas83秒结账询问特写，文字没有将其当作打断或付款。正式58PNG/6MP4尺寸/时长检查无问题，见openai-final-inventory.json；这仅作文件验证。前述视觉审阅及实际替代路径记录支持本轮采集处置结束，判为partial_with_public_source_gaps：1项图像完整、5项部分，硬件UNVERIFIED。用户要求未被改成全部素材完整。
