# 发布包维护规则

- 本仓库是 `手机 AI 与 2026 Q3 Agent 交互` 的公开、可下载静态调研包。
- `index.html`、`data/`、`docs/`、`media/` 必须保持相对链接可用，不引入服务器运行时依赖。
- 视频保持 H.264/yuv420p/faststart；横屏不超过 1280×720，竖屏不超过 720×1280。
- 不使用 Git LFS。单文件不得超过 100 MiB，发布包总量保持在 1 GiB 以下。
- 更新素材后必须同步 `manifest.json`、`SHA256SUMS.txt`，并完成本地链接、哈希和视频解码检查。
- 保留资料的来源、时间、市场、证据强度和缺口；不得把官方演示、第三方录屏或研究 PoC 写成实机验证。
- 不提交密钥、个人数据、浏览器缓存、临时转码文件或未引用原片。

